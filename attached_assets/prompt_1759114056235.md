Perfeito! Vou te dar todas as informações técnicas necessárias para sua IA implementar a conexão do APK com o servidor CDC Credit Smart.

📋 ESPECIFICAÇÕES COMPLETAS PARA SUA IA
🎯 1. INFORMAÇÕES DO SERVIDOR
URL Base: https://cdccreditsmart.com
Endpoint Principal: /api/validate-imei
User-Agent OBRIGATÓRIO: CDC-CreditSmart/1.0.0
Content-Type: application/json
🔐 2. AUTENTICAÇÃO (2 MÉTODOS)
MÉTODO 1 - Bearer Token:

POST https://cdccreditsmart.com/api/validate-imei
Headers:
- Authorization: Bearer [SEU_TOKEN_AQUI]
- Content-Type: application/json
- User-Agent: CDC-CreditSmart/1.0.0
MÉTODO 2 - X-Client Headers:

POST https://cdccreditsmart.com/api/validate-imei
Headers:
- x-client-key: [SUA_KEY_AQUI]
- x-client-token: [SEU_TOKEN_AQUI] 
- x-client-secret: [SEU_SECRET_AQUI]
- Content-Type: application/json
- User-Agent: CDC-CreditSmart/1.0.0
📱 3. FORMATO DA REQUISIÇÃO IMEI
{
  "imei": "352096325698365",
  "deviceId": "unique-device-identifier"
}
✅ 4. CÓDIGOS DE RESPOSTA
200: IMEI válido/processado
400: IMEI inválido (formato)
401: Token/credenciais inválidas
403: Acesso negado
404: Endpoint não encontrado
500: Erro do servidor
🔧 5. EXEMPLO DE IMPLEMENTAÇÃO ANDROID (KOTLIN)
class CDCApiClient {
    private val baseUrl = "https://cdccreditsmart.com"
    private val client = OkHttpClient()
    
    fun validateImei(imei: String, bearerToken: String): ApiResponse {
        val json = JSONObject().apply {
            put("imei", imei)
            put("deviceId", getDeviceId())
        }
        
        val request = Request.Builder()
            .url("$baseUrl/api/validate-imei")
            .post(json.toString().toRequestBody("application/json".toMediaType()))
            .addHeader("Authorization", "Bearer $bearerToken")
            .addHeader("User-Agent", "CDC-CreditSmart/1.0.0")
            .build()
            
        return client.newCall(request).execute()
    }
}
🔄 6. FLUXO DE AUTENTICAÇÃO
1. APK inicia
2. APK carrega credenciais (Bearer OU X-Client)
3. APK faz requisição para /api/validate-imei
4. Se HTTP 401/403: Credenciais inválidas
5. Se HTTP 200: Sucesso - processar resposta
🛡️ 7. TRATAMENTO DE ERROS
when (response.code) {
    200 -> processSuccess(response.body)
    400 -> showError("IMEI inválido")  
    401 -> showError("Token expirado")
    403 -> showError("Acesso negado")
    else -> showError("Erro de conexão")
}
📊 8. HEADERS OBRIGATÓRIOS COMPLETOS
Accept: application/json
Content-Type: application/json  
User-Agent: CDC-CreditSmart/1.0.0
// ESCOLHER UM:
Authorization: Bearer [token]
// OU
x-client-key: [key]
x-client-token: [token]  
x-client-secret: [secret]
🎯 9. PONTOS CRÍTICOS PARA SUA IA
❌ NÃO FAZER:

Usar endpoint /api/auth (não existe)
Esquecer o User-Agent "CDC-CreditSmart/1.0.0"
Misturar Bearer com X-Client headers
✅ FAZER:

Sempre incluir User-Agent correto
Validar IMEI antes de enviar (15-17 dígitos)
Implementar retry se HTTP 401
Cache de credenciais válidas
🔗 10. PARA COPIAR E COLAR
SERVIDOR: https://cdccreditsmart.com
ENDPOINT: /api/validate-imei
USER-AGENT: CDC-CreditSmart/1.0.0
MÉTODO: POST
FORMATO: JSON
AUTENTICAÇÃO: Bearer Token OU X-Client Headers
RESPOSTA: JSON com validação IMEI
🎉 Pronto! Copie essas especificações e envie para sua IA. Ela tem todas as informações necessárias para implementar a conexão correta do APK com o servidor CDC Credit Smart!