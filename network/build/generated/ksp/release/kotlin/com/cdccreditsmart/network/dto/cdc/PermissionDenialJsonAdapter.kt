// Code generated by moshi-kotlin-codegen. Do not edit.
@file:Suppress("DEPRECATION", "unused", "UNUSED_PARAMETER", "ClassName", "REDUNDANT_PROJECTION",
    "RedundantExplicitType", "LocalVariableName", "RedundantVisibilityModifier",
    "PLATFORM_CLASS_MAPPED_TO_KOTLIN", "IMPLICIT_NOTHING_TYPE_ARGUMENT_IN_RETURN_POSITION")

package com.cdccreditsmart.network.dto.cdc

import com.squareup.moshi.JsonAdapter
import com.squareup.moshi.JsonReader
import com.squareup.moshi.JsonWriter
import com.squareup.moshi.Moshi
import com.squareup.moshi.`internal`.Util
import java.lang.NullPointerException
import java.lang.reflect.Constructor
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.emptySet
import kotlin.jvm.Volatile
import kotlin.text.buildString

public class PermissionDenialJsonAdapter(
  moshi: Moshi,
) : JsonAdapter<PermissionDenial>() {
  private val options: JsonReader.Options = JsonReader.Options.of("permission", "reason",
      "canRetry", "retryAfter")

  private val stringAdapter: JsonAdapter<String> = moshi.adapter(String::class.java, emptySet(),
      "permission")

  private val booleanAdapter: JsonAdapter<Boolean> = moshi.adapter(Boolean::class.java, emptySet(),
      "canRetry")

  private val nullableLongAdapter: JsonAdapter<Long?> = moshi.adapter(Long::class.javaObjectType,
      emptySet(), "retryAfter")

  @Volatile
  private var constructorRef: Constructor<PermissionDenial>? = null

  public override fun toString(): String = buildString(38) {
      append("GeneratedJsonAdapter(").append("PermissionDenial").append(')') }

  public override fun fromJson(reader: JsonReader): PermissionDenial {
    var permission: String? = null
    var reason: String? = null
    var canRetry: Boolean? = false
    var retryAfter: Long? = null
    var mask0 = -1
    reader.beginObject()
    while (reader.hasNext()) {
      when (reader.selectName(options)) {
        0 -> permission = stringAdapter.fromJson(reader) ?: throw Util.unexpectedNull("permission",
            "permission", reader)
        1 -> reason = stringAdapter.fromJson(reader) ?: throw Util.unexpectedNull("reason",
            "reason", reader)
        2 -> {
          canRetry = booleanAdapter.fromJson(reader) ?: throw Util.unexpectedNull("canRetry",
              "canRetry", reader)
          // $mask = $mask and (1 shl 2).inv()
          mask0 = mask0 and 0xfffffffb.toInt()
        }
        3 -> {
          retryAfter = nullableLongAdapter.fromJson(reader)
          // $mask = $mask and (1 shl 3).inv()
          mask0 = mask0 and 0xfffffff7.toInt()
        }
        -1 -> {
          // Unknown name, skip it.
          reader.skipName()
          reader.skipValue()
        }
      }
    }
    reader.endObject()
    if (mask0 == 0xfffffff3.toInt()) {
      // All parameters with defaults are set, invoke the constructor directly
      return  PermissionDenial(
          permission = permission ?: throw Util.missingProperty("permission", "permission", reader),
          reason = reason ?: throw Util.missingProperty("reason", "reason", reader),
          canRetry = canRetry as Boolean,
          retryAfter = retryAfter
      )
    } else {
      // Reflectively invoke the synthetic defaults constructor
      @Suppress("UNCHECKED_CAST")
      val localConstructor: Constructor<PermissionDenial> = this.constructorRef ?:
          PermissionDenial::class.java.getDeclaredConstructor(String::class.java,
          String::class.java, Boolean::class.javaPrimitiveType, Long::class.javaObjectType,
          Int::class.javaPrimitiveType, Util.DEFAULT_CONSTRUCTOR_MARKER).also {
          this.constructorRef = it }
      return localConstructor.newInstance(
          permission ?: throw Util.missingProperty("permission", "permission", reader),
          reason ?: throw Util.missingProperty("reason", "reason", reader),
          canRetry,
          retryAfter,
          mask0,
          /* DefaultConstructorMarker */ null
      )
    }
  }

  public override fun toJson(writer: JsonWriter, value_: PermissionDenial?): Unit {
    if (value_ == null) {
      throw NullPointerException("value_ was null! Wrap in .nullSafe() to write nullable values.")
    }
    writer.beginObject()
    writer.name("permission")
    stringAdapter.toJson(writer, value_.permission)
    writer.name("reason")
    stringAdapter.toJson(writer, value_.reason)
    writer.name("canRetry")
    booleanAdapter.toJson(writer, value_.canRetry)
    writer.name("retryAfter")
    nullableLongAdapter.toJson(writer, value_.retryAfter)
    writer.endObject()
  }
}
