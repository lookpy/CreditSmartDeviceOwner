// Code generated by moshi-kotlin-codegen. Do not edit.
@file:Suppress("DEPRECATION", "unused", "UNUSED_PARAMETER", "ClassName", "REDUNDANT_PROJECTION",
    "RedundantExplicitType", "LocalVariableName", "RedundantVisibilityModifier",
    "PLATFORM_CLASS_MAPPED_TO_KOTLIN", "IMPLICIT_NOTHING_TYPE_ARGUMENT_IN_RETURN_POSITION")

package com.cdccreditsmart.network.api

import com.squareup.moshi.JsonAdapter
import com.squareup.moshi.JsonReader
import com.squareup.moshi.JsonWriter
import com.squareup.moshi.Moshi
import com.squareup.moshi.`internal`.Util
import java.lang.NullPointerException
import java.lang.reflect.Constructor
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.emptySet
import kotlin.jvm.Volatile
import kotlin.text.buildString

public class BiometryMetadataJsonAdapter(
  moshi: Moshi,
) : JsonAdapter<BiometryMetadata>() {
  private val options: JsonReader.Options = JsonReader.Options.of("cameraInfo", "environmentInfo",
      "captureTimestamp", "attempts")

  private val nullableCameraInfoAdapter: JsonAdapter<CameraInfo?> =
      moshi.adapter(CameraInfo::class.java, emptySet(), "cameraInfo")

  private val nullableEnvironmentInfoAdapter: JsonAdapter<EnvironmentInfo?> =
      moshi.adapter(EnvironmentInfo::class.java, emptySet(), "environmentInfo")

  private val longAdapter: JsonAdapter<Long> = moshi.adapter(Long::class.java, emptySet(),
      "captureTimestamp")

  private val intAdapter: JsonAdapter<Int> = moshi.adapter(Int::class.java, emptySet(), "attempts")

  @Volatile
  private var constructorRef: Constructor<BiometryMetadata>? = null

  public override fun toString(): String = buildString(38) {
      append("GeneratedJsonAdapter(").append("BiometryMetadata").append(')') }

  public override fun fromJson(reader: JsonReader): BiometryMetadata {
    var cameraInfo: CameraInfo? = null
    var environmentInfo: EnvironmentInfo? = null
    var captureTimestamp: Long? = null
    var attempts: Int? = null
    var mask0 = -1
    reader.beginObject()
    while (reader.hasNext()) {
      when (reader.selectName(options)) {
        0 -> {
          cameraInfo = nullableCameraInfoAdapter.fromJson(reader)
          // $mask = $mask and (1 shl 0).inv()
          mask0 = mask0 and 0xfffffffe.toInt()
        }
        1 -> {
          environmentInfo = nullableEnvironmentInfoAdapter.fromJson(reader)
          // $mask = $mask and (1 shl 1).inv()
          mask0 = mask0 and 0xfffffffd.toInt()
        }
        2 -> captureTimestamp = longAdapter.fromJson(reader) ?:
            throw Util.unexpectedNull("captureTimestamp", "captureTimestamp", reader)
        3 -> attempts = intAdapter.fromJson(reader) ?: throw Util.unexpectedNull("attempts",
            "attempts", reader)
        -1 -> {
          // Unknown name, skip it.
          reader.skipName()
          reader.skipValue()
        }
      }
    }
    reader.endObject()
    if (mask0 == 0xfffffffc.toInt()) {
      // All parameters with defaults are set, invoke the constructor directly
      return  BiometryMetadata(
          cameraInfo = cameraInfo,
          environmentInfo = environmentInfo,
          captureTimestamp = captureTimestamp ?: throw Util.missingProperty("captureTimestamp",
              "captureTimestamp", reader),
          attempts = attempts ?: throw Util.missingProperty("attempts", "attempts", reader)
      )
    } else {
      // Reflectively invoke the synthetic defaults constructor
      @Suppress("UNCHECKED_CAST")
      val localConstructor: Constructor<BiometryMetadata> = this.constructorRef ?:
          BiometryMetadata::class.java.getDeclaredConstructor(CameraInfo::class.java,
          EnvironmentInfo::class.java, Long::class.javaPrimitiveType, Int::class.javaPrimitiveType,
          Int::class.javaPrimitiveType, Util.DEFAULT_CONSTRUCTOR_MARKER).also {
          this.constructorRef = it }
      return localConstructor.newInstance(
          cameraInfo,
          environmentInfo,
          captureTimestamp ?: throw Util.missingProperty("captureTimestamp", "captureTimestamp",
              reader),
          attempts ?: throw Util.missingProperty("attempts", "attempts", reader),
          mask0,
          /* DefaultConstructorMarker */ null
      )
    }
  }

  public override fun toJson(writer: JsonWriter, value_: BiometryMetadata?): Unit {
    if (value_ == null) {
      throw NullPointerException("value_ was null! Wrap in .nullSafe() to write nullable values.")
    }
    writer.beginObject()
    writer.name("cameraInfo")
    nullableCameraInfoAdapter.toJson(writer, value_.cameraInfo)
    writer.name("environmentInfo")
    nullableEnvironmentInfoAdapter.toJson(writer, value_.environmentInfo)
    writer.name("captureTimestamp")
    longAdapter.toJson(writer, value_.captureTimestamp)
    writer.name("attempts")
    intAdapter.toJson(writer, value_.attempts)
    writer.endObject()
  }
}
